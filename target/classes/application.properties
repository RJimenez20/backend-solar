spring.application.name=backend
server.port=8080

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/gestorDeProyectos?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=123456
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
# We will just disable auto DDL since we provided a schema.sql, or we can set it to update.
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration
app.jwt.secret=94a08da1fecbb6e8b46990538c7b50b240cebc41b4b1a7741e3d060fb3ccfa4a
app.jwt.expiration=86400000

# CORS
app.cors.allowedOrigins=http://localhost:4200
